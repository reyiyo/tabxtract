target/
gen/
# The contents are generated by scripts/build_sidecar.py and
# scripts/fetch_native_deps.py, but the directories are committed:
# tauri.conf.json includes them by glob, and a glob matching nothing is a build
# error, not a warning.
sidecar/*
!sidecar/.gitkeep
native/*
!native/.gitkeep
